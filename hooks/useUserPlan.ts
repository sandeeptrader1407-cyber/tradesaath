/**
 * @deprecated — use `usePlan` from '@/lib/planStore' instead.
 * This file is kept temporarily for safety; delete after verifying build.
 */
export { usePlan as useUserPlan, type PlanType as PlanId } from '@/lib/planStore'
export type { PlanType } from '@/lib/planStore'
